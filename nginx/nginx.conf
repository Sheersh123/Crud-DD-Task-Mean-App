events {}

http {
  server {
    listen 80;

    # Frontend
    location / {
      proxy_pass http://frontend:80/;
    }

    # Backend API (adjust /api/ if your Angular uses another prefix)
    location /api/ {
      proxy_pass http://backend:8080/;
    }
  }
}
